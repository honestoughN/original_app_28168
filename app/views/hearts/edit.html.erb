<%= render "shared/header" %>
<main class="main">
  <div class="sign-up-box">
    <%= form_with model: @heart, local: true do |f| %>

    <%= render 'shared/error_messages', model: f.object %>

      <div>
        <%= f.label "タイトル" %><br />
        <%= f.text_field :title, class: "sign-up-box-list", placeholder: "タイトルを入力してください" %>
      </div>
      <div>
        <%= f.label "カテゴリー" %><br />
        <%= f.collection_select(:category_id, Category.all, :id, :name, {}, {class:"sign-up-box-list"}) %>
      </div>
      <div>
        <%= f.label "子カテゴリー" %><br />
        <%= f.text_field :category_id, class: "sign-up-box-list", placeholder: "子カテゴリーを選択してください" %>
      </div>
      <div>
        <%= f.label "あなたのことを教えて下さい♪" %><br />
        <%= f.text_area :detail, class: "sign-up-box-list", placeholder: "あなたの想いを自由に書いてください" %>
      </div>
      <div class="button">
        <%= f.submit "編集完了", class: "sign-up-button" %>
      </div>
    <% end %>
  </div>
</main>
<%= render "shared/footer" %>
